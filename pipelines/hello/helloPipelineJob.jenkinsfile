#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('Print greeting') {
            steps {
                echo("${GREETING}, ${NAME}!")
                echo("Previous name: ${PREV_NAME}")
                script {
                    globalEnv.set('PREV_NAME', NAME)
                }
            }
        }
    }
}
