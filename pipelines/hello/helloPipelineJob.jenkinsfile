#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('Print greeting') {
            steps {
                echo("${GREETING}, ${NAME}!")
                script {
                    def var = 'foobar'
                    echo(var)
                }
            }
        }
    }
}
